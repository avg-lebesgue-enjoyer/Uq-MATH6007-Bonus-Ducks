% \begin{frame}{Refresher: Categories, Functors and Natural Transformation} % .;
  
%   \begin{block}{Definition: Category}
%     \par A (locally small) \textbf{category} $\mathcal{C}$ consists of:
%     \begin{itemize}[nosep]
%       \item[$\bullet$] A collection $\ob(\C)$ of \textbf{objects};
%       \item[$\bullet$] For each $a, b \in \C$, a set $\C(a, b)$ of \textbf{arrows} $a \to b$;
%       \item[$\bullet$] For each $a \in \C$, an \textbf{identity arrow} $1_a \in \C(a, a)$;
%       \item[$\bullet$] {
%         For each $a, b, c \in \C$, a \textbf{composition operation}
%         \vspace{-0.5 \baselineskip}
%         \begin{align*}
%           {\blank \circ \blank} : \C(b, c) \times \C(a, b) \longrightarrow \C(a, c)
%         \end{align*}
%       }
%     \end{itemize}
%     \vspace{-\baselineskip}
%     satisfying the following for all $a \xrightarrow{f} b \xrightarrow{g} c \xrightarrow{h} d$ in $\C$:
%     \vspace{-0.5 \baselineskip}
%     \begin{align*}
%       f \circ 1_a &= f
%       & 1_b \circ f &= f
%       & (h \circ g) \circ f &= h \circ (g \circ f)
%     \end{align*}
%   \end{block}

% \end{frame}

\begin{frame}[t]{Refresher: Categories} % .;

  \par\attentive{Definition:} Categories
  \vspace{5\baselineskip}
  \par\attentive{Examples:}
  
\end{frame}

\begin{frame}[t]{Refresher: Functors} % .;

  \par\attentive{Definition:} Functors
  \vspace{5\baselineskip}
  \par\attentive{Examples:}
  
\end{frame}
